# Пошаговый гайд для начинающих (Windows)

## 🎯 Цель

Этот гайд поможет вам **с нуля** запустить проект многослойного перцептрона для классификации медицинских изображений на компьютере с Windows.

**Не требуется:**
- Опыт программирования
- Знание командной строки
- Предустановленное ПО (кроме Windows)

**Время выполнения:** 30-40 минут

---

## 📋 Что нужно подготовить

1. **Компьютер с Windows** (Windows 10 или 11)
2. **Подключение к интернету** (~500 MB для загрузки)
3. **Свободное место на диске:** ~2 GB

---

## Шаг 1: Установка Python (10 минут)

### 1.1 Скачайте Python

1. Откройте браузер
2. Перейдите на: https://www.python.org/downloads/
3. Нажмите большую желтую кнопку "Download Python 3.x.x"
4. Подождите пока файл скачается (обычно называется `python-3.xx.x-amd64.exe`)

### 1.2 Установите Python

1. **Найдите скачанный файл** (обычно в папке "Загрузки")
2. **Запустите установщик** (двойной клик)
3. **⚠️ ВАЖНО:** Внизу окна **ОБЯЗАТЕЛЬНО поставьте галочку** "Add Python to PATH"
   ```
   ☑ Add Python 3.xx to PATH  <-- Это критически важно!
   ```
4. Нажмите "Install Now"
5. Дождитесь завершения установки (2-3 минуты)
6. Нажмите "Close"

### 1.3 Проверка установки

1. Нажмите `Win + R`
2. Введите: `cmd`
3. Нажмите Enter
4. В черном окне введите: `python --version`
5. Должно появиться: `Python 3.xx.x`

**Если появилась ошибка:**
- Переустановите Python
- НЕ ЗАБУДЬТЕ поставить галочку "Add Python to PATH"!

---

## Шаг 1.5: GPU ускорение (опционально, для продвинутых)

### Нужно ли мне GPU?

**НЕТ, если:**
- У вас нет NVIDIA видеокарты
- Вы впервые работаете с машинным обучением
- Вы просто хотите запустить проект

**ДА, если:**
- У вас есть NVIDIA GeForce/RTX/Quadro
- Вы хотите ускорить обучение в 2-3 раза
- Вы готовы потратить 15 минут на настройку

### Как включить GPU (NVIDIA CUDA)

#### 1. Проверьте вашу видеокарту:

1. Нажмите `Win + R`
2. Введите: `dxdiag`
3. Вкладка "Экран" → смотрите "Имя"
4. Если там NVIDIA GeForce/RTX - можете использовать GPU

#### 2. Установите CUDA Toolkit:

1. Перейдите: https://developer.nvidia.com/cuda-downloads
2. Выберите: Windows → x86_64 → версию Windows → exe (local)
3. Скачайте установщик (~3 GB)
4. Запустите установку (примите настройки по умолчанию)
5. Дождитесь завершения (~10 минут)

#### 3. Переустановите PyTorch с CUDA:

1. Откройте командную строку (`Win + R` → `cmd`)
2. Выполните команды:
   ```cmd
   pip uninstall torch torchvision
   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
   ```
3. Дождитесь установки (~5 минут)

#### 4. Проверьте GPU:

```cmd
python -c "import torch; print('CUDA:', torch.cuda.is_available())"
```

Должно показать: `CUDA: True`

### Важно:

- CPU версия **тоже работает** - просто медленнее
- Все скрипты **автоматически** определят GPU и будут использовать его
- Если GPU не определяется - обучение пойдет на CPU
- Для студентов **необязательно** использовать GPU для сдачи задания

---

## Шаг 2: Скачивание проекта (5 минут)

### Если у вас есть Git:

1. Откройте командную строку (см. Шаг 1.3)
2. Перейдите в нужную папку:
   ```cmd
   cd C:\Users\ВашеИмя\Documents
   ```
3. Клонируйте репозиторий:
   ```cmd
   git clone [адрес репозитория]
   cd 1_MLP
   ```

### Если Git нет (проще):

1. Скачайте проект как ZIP-архив
2. Распакуйте в удобную папку (например, `C:\Users\ВашеИмя\Documents\1_MLP`)
3. Запомните путь к папке!

---

## Шаг 3: Запуск проекта (15 минут)

### 3.1 Откройте папку проекта

1. Откройте "Проводник" (Win + E)
2. Найдите папку `1_MLP`
3. Вы должны видеть файлы: `START.bat`, `README.md`, папки `scripts`, `notebooks`

### 3.2 Запустите главное меню

1. **Двойной клик** на файл `START.bat`
2. Откроется черное окно с меню:

```
===================================================================
  МНОГОСЛОЙНЫЙ ПЕРЦЕПТРОН - КЛАССИФИКАЦИЯ ORGANCMNIST
===================================================================

  1. Установить зависимости (первый запуск)
  2. Загрузить датасет
  3. Запустить Jupyter Notebook
  4. Обучить baseline модель
  5. Запустить эксперименты (быстрые)
  ...
===================================================================
```

### 3.3 Установите зависимости

1. Введите цифру `1` и нажмите Enter
2. Подождите 5-10 минут (скачиваются библиотеки)
3. Вы увидите много текста - это нормально!
4. В конце должно быть: `[УСПЕХ] Все зависимости установлены!`

**Если появились ошибки:**
- Проверьте интернет-соединение
- Перезапустите `START.bat`
- Попробуйте пункт `1` снова

### 3.4 Загрузите датасет

1. В меню введите цифру `2` и нажмите Enter
2. Подождите 2-3 минуты (скачивается ~100 MB)
3. Датасет сохранится в папку `C:\Users\ВашеИмя\.medmnist\`
4. В конце: `[УСПЕХ] Датасет готов к использованию!`

**Если загрузка не удалась:**
- Скрипт автоматически попробует альтернативный метод
- Если и это не помогло - следуйте инструкциям на экране для ручной загрузки

---

## Шаг 4: Работа с проектом

### Вариант A: Jupyter Notebook (рекомендуется для начинающих)

1. В меню выберите пункт `3`
2. Откроется браузер с Jupyter
3. Кликните на: `notebooks` → `mlp_organcmnist.ipynb`
4. Вы увидите ноутбук с кодом и текстом
5. Для запуска ячейки: нажмите `Shift + Enter`
6. Запускайте ячейки по порядку сверху вниз

**Совет:** Читайте текстовые блоки - там объяснения!

### Вариант B: Обучение через скрипты

#### B.1 Baseline модель (быстро, 5-10 мин)

1. В меню выберите пункт `4`
2. Прочитайте информацию, нажмите Enter
3. Наблюдайте за процессом обучения:
   ```
   Epoch 1/20 - Train: 45.23% / Val: 48.15%
   Epoch 2/20 - Train: 58.14% / Val: 61.23%
   ...
   ```
4. По завершении откроются графики
5. Результаты: `results/baseline_results/`

#### B.2 Быстрые эксперименты (8 минут)

1. В меню выберите пункт `5`
2. Запустятся 6 экспериментов по 10 эпох
3. Результаты: `results/experiments_results/quick_test_summary.txt`

#### B.3 Полные эксперименты (40 минут)

1. В меню выберите пункт `6`
2. **ВНИМАНИЕ:** Займет ~40 минут!
3. Можно оставить компьютер работать
4. Результаты: `results/experiments_results/`

---

## Шаг 5: Просмотр результатов

### 5.1 Через меню

1. Выберите пункт `7` в START.bat
2. Откроется папка `results/`
3. Посмотрите файлы:
   - `*.png` - графики (открываются двойным кликом)
   - `*.txt` - текстовые отчеты (открываются Блокнотом)

### 5.2 Документация

1. Выберите пункт `8` в START.bat
2. Читайте:
   - `README.md` - общее описание
   - `docs/guide.md` - подробный гайд
   - `docs/final_report.txt` - итоговый отчет

---

## 🔧 Решение проблем

### Проблема: "Python не найден"

**Решение:**
1. Переустановите Python
2. **НЕ ЗАБУДЬТЕ галочку "Add Python to PATH"!**
3. Перезагрузите компьютер

### Проблема: "Датасет не загружается"

**Решение:**
1. Проверьте интернет
2. Скрипт попробует альтернативный метод
3. Если не помогло - скачайте вручную:
   - URL: https://zenodo.org/records/10519652/files/organcmnist.npz?download=1
   - Сохраните в: `C:\Users\ВашеИмя\.medmnist\organcmnist.npz`

### Проблема: "Jupyter не открывается"

**Решение:**
1. Закройте все окна
2. В START.bat выберите пункт `1` (переустановить зависимости)
3. Попробуйте снова пункт `3`

### Проблема: "Обучение останавливается с ошибкой"

**Решение:**
1. Проверьте что датасет загружен (пункт `2` в меню)
2. Убедитесь что у вас достаточно места на диске (~2 GB)
3. Закройте другие программы (освободите память)

### Проблема: "Очень долго обучается"

**Это нормально!**
- Baseline: 5-10 минут (CPU) / 2-3 минуты (GPU)
- Быстрые эксперименты: ~8 минут (CPU) / ~3 минуты (GPU)
- Полные эксперименты: ~40 минут (CPU) / ~15 минут (GPU)

Время зависит от вашего компьютера.

**Если хотите ускорить - используйте GPU (см. Шаг 1.5).**

### Проблема: "CUDA не работает / не определяется GPU"

**Возможные причины:**

1. **Драйверы NVIDIA устарели**
   - Откройте GeForce Experience или скачайте с nvidia.com
   - Установите последние драйверы
   - Перезагрузите компьютер

2. **Несовместимость версии CUDA**
   - CUDA 12.1 требует драйверы 530+
   - Проверьте версию: `nvidia-smi` в командной строке
   - Если драйверы старые - используйте CUDA 11.8:
     ```cmd
     pip uninstall torch torchvision
     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
     ```

3. **Нет NVIDIA видеокарты**
   - AMD GPU с PyTorch работают плохо на Windows
   - Intel GPU не поддерживаются
   - **Решение: используйте CPU** (это нормально!)

4. **Проблемы установки CUDA Toolkit**
   - Попробуйте без Toolkit - только PyTorch:
     ```cmd
     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
     ```
   - PyTorch включает все необходимое

**Если ничего не помогло:**
- **Используйте CPU** - он работает на любом ПК
- Для учебного проекта скорость не критична
- Задание выполняется и на CPU

---

## 📖 Что делать дальше

### 1. Изучите ноутбук

- Откройте `mlp_organcmnist.ipynb` через пункт `3`
- Читайте комментарии
- Запускайте ячейки по порядку
- Экспериментируйте с параметрами

### 2. Прочитайте документацию

- `docs/guide.md` - объясняет каждый шаг подробно
- `docs/final_report.txt` - итоговый анализ всех экспериментов

### 3. Посмотрите результаты

- Откройте папку `results/`
- Изучите графики обучения
- Сравните разные конфигурации

---

## ✅ Чек-лист готовности

Перед сдачей проверьте:

- [ ] Python установлен (команда `python --version` работает)
- [ ] Все зависимости установлены (пункт `1` в START.bat)
- [ ] Датасет загружен (пункт `2` в START.bat)
- [ ] Baseline модель обучена (пункт `4` в START.bat)
- [ ] Хотя бы быстрые эксперименты запущены (пункт `5`)
- [ ] Все результаты в папке `results/`
- [ ] Ноутбук открывается и запускается (пункт `3`)
- [ ] Прочитана документация (пункт `8`)

---

## 🆘 Нужна помощь?

### Документация проекта:
1. `README.md` - общее описание
2. `docs/guide.md` - подробный технический гайд
3. `docs/final_report.txt` - итоговый отчет

### Проверьте:
1. Python установлен правильно (с PATH)
2. Интернет работает
3. Достаточно места на диске
4. Все скрипты .bat запускаются из папки проекта

---

## 📌 Краткая шпаргалка

**Первый запуск:**
```
1. Установите Python (с галочкой "Add to PATH"!)
2. Двойной клик START.bat
3. Выберите: 1 → 2 → 3
4. Готово!
```

**Для работы:**
```
- START.bat → пункт 3 = Jupyter Notebook
- START.bat → пункт 4 = Обучить baseline
- START.bat → пункт 7 = Посмотреть результаты
```

**Если что-то не работает:**
```
1. Перезапустите START.bat
2. Попробуйте пункт 1 (переустановить зависимости)
3. Проверьте интернет и место на диске
4. Прочитайте раздел "Решение проблем" выше
```

---

## ✨ Успехов в обучении!

Этот гайд создан для того, чтобы ЛЮБОЙ человек мог запустить проект, даже без опыта программирования. Если у вас что-то не получилось - перечитайте инструкции внимательно, обычно решение рядом!
