# Подробный гайд по проекту: Многослойный перцептрон для классификации OrganCMNIST

## Оглавление
1. [Введение](#введение)
2. [Структура проекта](#структура-проекта)
3. [Установка и настройка](#установка-и-настройка)
4. [Подробное описание ноутбука](#подробное-описание-ноутбука)
5. [Скрипты и их использование](#скрипты-и-их-использование)
6. [Интерпретация результатов](#интерпретация-результатов)
7. [Часто задаваемые вопросы](#часто-задаваемые-вопросы)

---

## Введение

### Что такое многослойный перцептрон (MLP)?

**Многослойный перцептрон** - это тип искусственной нейронной сети, состоящий из нескольких слоев нейронов:
- **Входной слой**: принимает данные (в нашем случае - пиксели изображения)
- **Скрытые слои**: обрабатывают данные и извлекают признаки
- **Выходной слой**: выдает предсказание (класс объекта)

### Наша задача

Мы обучаем MLP классифицировать медицинские изображения органов из датасета **OrganCMNIST**:
- **11 классов**: bladder (мочевой пузырь), femur (бедренная кость), heart (сердце), kidney (почка), liver (печень), lung (легкое), pancreas (поджелудочная), spleen (селезенка)
- **Размер изображений**: 28×28 пикселей (grayscale)
- **Цель**: максимизировать точность классификации

### Почему это важно?

Медицинская классификация изображений - это основа для:
- Автоматической диагностики заболеваний
- Помощи врачам в принятии решений
- Ускорения процесса обработки медицинских данных

---

## Структура проекта

```
1_MLP/
├── notebooks/
│   └── mlp_organcmnist.ipynb    # Основной ноутбук с экспериментами
│
├── scripts/
│   ├── train_mlp.py             # Обучение baseline модели
│   ├── experiments.py           # Полные эксперименты
│   ├── experiments_quick.py     # Быстрый тест
│   ├── visualize_results.py     # Визуализация результатов
│   └── generate_report.py       # Генерация отчета
│
├── results/
│   ├── baseline_results/        # Результаты baseline
│   └── experiments_results/     # Результаты всех экспериментов
│
├── docs/
│   ├── final_report.txt        # Итоговый отчет
│   └── guide.md               # Этот файл
│
├── README.md                   # Краткое описание
└── requirements.txt           # Зависимости
```

---

## Установка и настройка

### Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

**Что устанавливается:**
- `torch` - библиотека PyTorch для глубокого обучения
- `torchvision` - инструменты для работы с изображениями
- `medmnist` - датасет медицинских изображений
- `numpy` - работа с массивами
- `matplotlib` - визуализация
- `pandas` - работа с таблицами
- `scikit-learn` - инструменты машинного обучения

### Шаг 2: Скачивание датасета

Датасет автоматически скачивается в `~/.medmnist/` при первом запуске.

**Если возникают проблемы:**
```bash
mkdir -p ~/.medmnist
curl -L "https://zenodo.org/records/10519652/files/organcmnist.npz?download=1" -o ~/.medmnist/organcmnist.npz
```

### Шаг 3: Проверка установки

```bash
python -c "import torch; print(f'PyTorch: {torch.__version__}')"
python -c "import medmnist; print(f'MedMNIST: {medmnist.__version__}')"
```

---

## Подробное описание ноутбука

### Параграф 1: Импорт библиотек и настройка окружения

**Что происходит:**
```python
import torch  # Основная библиотека для нейронных сетей
import torch.nn as nn  # Модули для построения сетей
import torch.optim as optim  # Оптимизаторы (алгоритмы обучения)
```

**Зачем нужен SEED:**
```python
SEED = 42
torch.manual_seed(SEED)
```
Это обеспечивает **воспроизводимость** - каждый раз эксперимент будет давать одинаковый результат.

**Определение устройства:**
```python
device = torch.device('cuda')  # GPU NVIDIA
device = torch.device('mps')   # GPU Apple (M1/M2)
device = torch.device('cpu')   # Процессор
```
GPU ускоряет обучение в 10-100 раз по сравнению с CPU.

---

### Параграф 2: Загрузка и анализ датасета

**Что такое датасет OrganCMNIST:**
- Медицинские изображения органов
- 11 классов (типов органов)
- Разделен на 3 части:
  - **Train (обучающая)**: 12,975 изображений - для обучения модели
  - **Validation (валидационная)**: 2,392 изображения - для подбора параметров
  - **Test (тестовая)**: 8,216 изображений - для финальной оценки

**Зачем нужна нормализация:**
```python
transforms.Normalize(mean=[0.5], std=[0.5])
```
Приводит значения пикселей из диапазона [0, 1] в [-1, 1]. Это помогает модели быстрее обучаться.

**Визуализация:**
Показываем примеры изображений, чтобы понять:
- Как выглядят данные
- Есть ли особенности
- Качество изображений

---

### Параграф 3: Реализация модели MLP

**Архитектура модели:**
```
Вход: 784 нейрона (28×28 пикселей)
    ↓
Скрытый слой 1: 128 нейронов
    ↓ ReLU (функция активации)
    ↓ Dropout (случайное отключение нейронов)
    ↓
Скрытый слой 2: 64 нейрона
    ↓ ReLU
    ↓
Выход: 11 нейронов (вероятности классов)
```

**Ключевые компоненты:**

1. **Linear (Линейный слой)**
   ```python
   nn.Linear(in_features, out_features)
   ```
   Выполняет умножение матриц: `y = x @ W + b`

2. **ReLU (Rectified Linear Unit)**
   ```python
   nn.ReLU()
   ```
   Функция активации: `f(x) = max(0, x)`
   - Убирает отрицательные значения
   - Добавляет нелинейность в модель

3. **Dropout**
   ```python
   nn.Dropout(p=0.3)
   ```
   Случайно отключает 30% нейронов при обучении
   - Предотвращает переобучение
   - Заставляет сеть учиться более устойчивым признакам

4. **Batch Normalization**
   ```python
   nn.BatchNorm1d(features)
   ```
   Нормализует выходы слоя
   - Ускоряет обучение
   - Стабилизирует процесс

---

### Параграф 4: Функции обучения и оценки

**Цикл обучения (одна эпоха):**

1. **Forward pass (прямой проход)**
   ```python
   outputs = model(images)  # Получаем предсказания
   ```
   Данные проходят через все слои сети

2. **Вычисление ошибки (Loss)**
   ```python
   loss = criterion(outputs, labels)
   ```
   Сравниваем предсказания с правильными ответами
   - **CrossEntropyLoss** - стандартная функция потерь для классификации

3. **Backward pass (обратный проход)**
   ```python
   loss.backward()  # Вычисляем градиенты
   ```
   Определяем, как изменить веса, чтобы уменьшить ошибку

4. **Обновление весов**
   ```python
   optimizer.step()  # Применяем изменения
   ```
   Оптимизатор (Adam) обновляет веса сети

**Метрики:**
- **Loss (потери)**: насколько модель ошибается (чем меньше, тем лучше)
- **Accuracy (точность)**: % правильных предсказаний (чем больше, тем лучше)

---

### Параграф 5: Baseline модель

**Что такое baseline:**
Это простая модель для сравнения. Все улучшения оцениваются относительно baseline.

**Наш baseline:**
- Архитектура: [128, 64]
- Без регуляризации (dropout=0)
- Без batch normalization
- ReLU активация
- Adam оптимизатор, lr=0.001

**Результат baseline:**
- Test Accuracy: **74.84%**

**Проблема:**
Модель **переобучается**:
- Train Accuracy: 99.56% (отлично запомнила обучающие данные)
- Test Accuracy: 74.84% (плохо обобщает на новые данные)

**Решение:** Эксперименты с регуляризацией!

---

### Параграф 6: Эксперименты с гиперпараметрами

#### Эксперимент 1: Архитектура

**Что тестируем:**
- Small [64, 32] - мало параметров
- Baseline [128, 64] - средний размер
- Large [256, 128] - много параметров
- Deep [128, 128, 64] - больше слоев

**Вывод:**
Больше параметров ≠ лучше качество!
- Слишком маленькая модель: недостаточно выразительности
- Слишком большая модель: переобучение

#### Эксперимент 2: Регуляризация

**Dropout:**
```python
dropout=0.3  # Отключаем 30% нейронов
```
**Эффект:** Снижает переобучение, модель лучше обобщает

**Weight Decay (L2 регуляризация):**
```python
weight_decay=1e-4  # Штрафуем большие веса
```
**Эффект:** Веса остаются маленькими, модель проще

**Результат:**
- Dropout 0.3: 76.62% test accuracy (+1.78%)
- L2 1e-3: 76.44% test accuracy (+1.60%)

#### Эксперимент 3: Batch Normalization

**Что делает:**
Нормализует активации между слоями

**Эффект:**
- Ускоряет обучение
- Позволяет использовать больший learning rate
- Сама по себе работает как регуляризация

**Результат:**
- BN alone: 77.29% test accuracy (+2.45%)
- **BN + Dropout 0.3: 78.33% test accuracy (+3.49%)** ✨ ЛУЧШИЙ!

#### Эксперимент 4: Функции активации

**ReLU:**
```python
f(x) = max(0, x)
```
Простая и эффективная

**LeakyReLU:**
```python
f(x) = x if x > 0 else 0.1*x
```
Пропускает немного отрицательных значений

**ELU:**
```python
f(x) = x if x > 0 else α(e^x - 1)
```
Гладкая для отрицательных значений

**Результат:**
Все примерно одинаковы, ReLU - надежный выбор.

#### Эксперимент 5: Learning Rate и Schedulers

**Learning Rate (скорость обучения):**
- Слишком большой: модель не сходится, прыгает
- Слишком маленький: обучение очень медленное
- Оптимальный: ~0.001 для Adam

**Scheduler:**
Изменяет LR во время обучения:
```python
# StepLR: уменьшает LR каждые N эпох
scheduler = optim.lr_scheduler.StepLR(optimizer, step_size=10, gamma=0.5)

# CosineAnnealing: плавно уменьшает LR
scheduler = optim.lr_scheduler.CosineAnnealingLR(optimizer, T_max=epochs)
```

---

### Параграф 7: Лучшая модель

**Итоговая конфигурация:**
- Архитектура: [128, 64]
- Batch Normalization: ✓
- Dropout: 0.3
- Activation: ReLU
- Optimizer: Adam (lr=0.001)
- Epochs: 20

**Результаты:**
- Validation Accuracy: **91.22%**
- Test Accuracy: **78.33%**
- Улучшение vs baseline: **+3.49%**

---

### Параграф 8: Выводы

**Ключевые находки:**

1. **Регуляризация критична**
   - Без регуляризации: сильное переобучение
   - С регуляризацией: лучше обобщение

2. **Batch Normalization эффективна**
   - Ускоряет обучение
   - Улучшает качество

3. **Комбинация техник работает лучше**
   - BN + Dropout > BN > Dropout > Baseline

4. **Архитектура имеет значение**
   - Но не так сильно как регуляризация
   - [128, 64] - хороший баланс

**Проблемы:**
- Val-Test gap: 91.22% → 78.33% (разница 12.89%)
- Возможно, распределение данных различается
- Или переобучение под validation set

**Что можно улучшить:**
- Использовать CNN (свёрточные сети) вместо MLP
- Применить аугментацию данных
- Попробовать ансамблирование
- Early stopping
- Cross-validation

---

## Скрипты и их использование

### 1. train_mlp.py - Обучение baseline

**Назначение:** Быстро обучить baseline модель

**Запуск:**
```bash
python scripts/train_mlp.py
```

**Что делает:**
1. Загружает датасет
2. Создает простую MLP [128, 64]
3. Обучает 20 эпох
4. Сохраняет результаты в `results/baseline_results/`

**Результаты:**
- `baseline_training.png` - графики loss/accuracy
- `baseline_model.pth` - веса модели
- `baseline_results.txt` - текстовый отчет

**Время:** ~5-10 минут

---

### 2. experiments.py - Полные эксперименты

**Назначение:** Систематический поиск лучших гиперпараметров

**Запуск:**
```bash
python -u scripts/experiments.py
```

**Что делает:**
- Запускает 24 различных конфигурации
- Каждая по 20 эпох
- Сохраняет все результаты

**Конфигурации:**
1. Архитектура (5 вариантов)
2. Регуляризация (7 вариантов)
3. Batch Norm (3 варианта)
4. Активации (4 варианта)
5. Learning Rate (5 вариантов)

**Результаты:**
- `all_experiments.json` - полные данные
- `summary.txt` - сводная таблица
- Лучшая модель

**Время:** ~30-40 минут

---

### 3. experiments_quick.py - Быстрый тест

**Назначение:** Проверить что все работает

**Запуск:**
```bash
python -u scripts/experiments_quick.py
```

**Что делает:**
- 6 экспериментов по 10 эпох
- По одному из каждой категории

**Результаты:**
- `quick_test.json`
- `quick_test_summary.txt`

**Время:** ~6-8 минут

---

### 4. visualize_results.py - Визуализация

**Назначение:** Создать графики и таблицы

**Запуск:**
```bash
python scripts/visualize_results.py
```

**Требования:**
Сначала нужно запустить `experiments.py`

**Что создает:**
- `overall_summary.png` - общая статистика
- `categories_comparison.png` - сравнение по категориям
- `correlation_heatmap.png` - корреляции
- `all_results.csv` - таблица для Excel

---

### 5. generate_report.py - Отчет

**Назначение:** Создать текстовый отчет

**Запуск:**
```bash
python scripts/generate_report.py
```

**Что создает:**
- `docs/final_report.txt` - полный отчет с:
  - Описанием baseline
  - Лучшей моделью
  - Результатами по категориям
  - Выводами

---

## Интерпретация результатов

### Как читать графики обучения

**Loss (потери):**
- **Уменьшается** - модель учится
- **Train loss ↓, Val loss ↑** - переобучение!
- **Обе уменьшаются** - хорошо

**Accuracy (точность):**
- **Увеличивается** - модель учится
- **Train acc ↑, Val acc стагнирует** - переобучение
- **Обе растут** - отлично

### Признаки переобучения

1. **Большой разрыв Train/Val:**
   - Train: 99%, Val: 75% ❌
   - Train: 85%, Val: 80% ✓

2. **Val loss растет:**
   ```
   Epoch 1: val_loss=0.8
   Epoch 5: val_loss=0.5  # Хорошо
   Epoch 10: val_loss=0.7  # Плохо! Переобучение
   ```

3. **Val accuracy падает:**
   ```
   Epoch 5: val_acc=89%  # Лучший результат
   Epoch 10: val_acc=87%  # Ухудшение
   ```

### Как бороться с переобучением

1. **Dropout** - отключает нейроны
2. **L2 regularization** - штрафует большие веса
3. **Batch Normalization** - нормализует активации
4. **Early Stopping** - останавливает при ухудшении val
5. **Data Augmentation** - увеличивает разнообразие данных
6. **Меньше параметров** - упрощает модель

---

## Часто задаваемые вопросы

### Q1: Почему Test Accuracy ниже Val Accuracy?

**A:** Несколько причин:
1. **Разное распределение данных** в val и test
2. **Переобучение под validation set** при подборе гиперпараметров
3. **Случайность** - возможно просто не повезло

**Решение:** Использовать cross-validation

---

### Q2: Как выбрать learning rate?

**A:** Стратегии:
1. **Grid search:** тестируем [0.0001, 0.001, 0.01, 0.1]
2. **Learning rate finder:** постепенно увеличиваем и смотрим на loss
3. **Правило большого пальца:**
   - Adam: 0.001
   - SGD: 0.01-0.1

---

### Q3: Сколько эпох обучать?

**A:** Зависит от задачи:
- **Наша задача:** 20 эпох достаточно
- **Признак:** Val accuracy перестала расти
- **Early stopping:** автоматически остановит

---

### Q4: Почему MLP, а не CNN?

**A:** Образовательные цели:
- MLP проще понять
- CNN лучше для изображений (использует пространственную структуру)
- **Следующий проект:** будет CNN!

---

### Q5: Как интерпретировать confusion matrix?

**A:** Confusion matrix показывает:
- **Диагональ** - правильные предсказания
- **Вне диагонали** - ошибки
- Какие классы путает модель

Пример:
```
           Predicted
           Heart  Lung
Actual
Heart      100    5      # 100 правильно, 5 спутали с lung
Lung        3    97      # 3 спутали с heart, 97 правильно
```

---

### Q6: Что делать если модель не обучается?

**Чеклист:**
1. ✓ Проверить данные (есть ли пропуски, правильные ли метки)
2. ✓ Проверить размерности (входы и выходы совпадают?)
3. ✓ Уменьшить learning rate
4. ✓ Убрать регуляризацию на время
5. ✓ Попробовать другой оптимизатор
6. ✓ Проверить что loss уменьшается (хотя бы на train)

---

### Q7: Как сохранить и загрузить модель?

**Сохранение:**
```python
torch.save(model.state_dict(), 'model.pth')
```

**Загрузка:**
```python
model = MLP(...)  # Создать модель с той же архитектурой
model.load_state_dict(torch.load('model.pth'))
model.eval()  # Переключить в режим inference
```

---

### Q8: Что такое batch size и как его выбрать?

**Batch size** - сколько примеров обрабатывается за раз

**Влияние:**
- **Маленький (16-32):**
  - + Больше обновлений весов
  - + Лучше обобщение
  - - Медленнее обучение
  - - Шумные градиенты

- **Большой (256-512):**
  - + Быстрее обучение
  - + Стабильные градиенты
  - - Хуже обобщение
  - - Требует больше памяти

**Наш выбор:** 128 - хороший баланс

---

### Q9: Зачем нужна валидационная выборка?

**Train:** Обучаем модель
**Validation:** Подбираем гиперпараметры (dropout, lr, архитектуру)
**Test:** Финальная оценка (ТОЛЬКО ОДИН РАЗ!)

**Почему нельзя использовать test для подбора:**
Это будет "подгонка под test" → завышенная оценка качества

---

### Q10: Что дальше?

**Следующие шаги:**
1. ✓ Изучить **CNN** (Convolutional Neural Networks)
2. ✓ Попробовать **Transfer Learning** (использовать предобученные модели)
3. ✓ Изучить **Data Augmentation** (повороты, масштабирование)
4. ✓ Попробовать **Ensemble** (комбинировать несколько моделей)
5. ✓ Изучить **Attention mechanisms**

---

## Полезные ресурсы

### Документация
- [PyTorch Documentation](https://pytorch.org/docs/)
- [MedMNIST](https://medmnist.com/)
- [Dive into Deep Learning](https://d2l.ai/)

### Курсы
- [Fast.ai Practical Deep Learning](https://course.fast.ai/)
- [Stanford CS231n](http://cs231n.stanford.edu/)
- [Deep Learning Specialization (Coursera)](https://www.coursera.org/specializations/deep-learning)

### Книги
- "Deep Learning" by Goodfellow, Bengio, Courville
- "Hands-On Machine Learning" by Aurélien Géron
- "Neural Networks and Deep Learning" by Michael Nielsen (free online)

---

## Глоссарий

**Accuracy (Точность)** - процент правильных предсказаний

**Batch** - группа примеров, обрабатываемых вместе

**Epoch (Эпоха)** - один полный проход по всем обучающим данным

**Gradient (Градиент)** - направление наибольшего изменения функции

**Learning Rate (Скорость обучения)** - размер шага при обновлении весов

**Loss (Потери)** - мера ошибки модели

**Overfitting (Переобучение)** - модель слишком хорошо запомнила обучающие данные

**Regularization (Регуляризация)** - методы борьбы с переобучением

**Validation (Валидация)** - проверка модели на данных, не используемых при обучении

**Weight (Вес)** - обучаемый параметр нейронной сети

---

## Заключение

Этот проект демонстрирует полный цикл работы с нейронными сетями:
1. ✓ Загрузка и анализ данных
2. ✓ Построение модели
3. ✓ Обучение и валидация
4. ✓ Эксперименты с гиперпараметрами
5. ✓ Анализ результатов
6. ✓ Выводы и рекомендации

**Главный урок:** Систематический подход к экспериментам важнее случайных попыток!

**Удачи в изучении глубокого обучения!** 🚀
